{
  "name": "webdriver-selenium-standalone",
  "version": "1.0.0",
  "description": "```bash mkdir test cd test npm init -y npm install --save-dev webdriverio selenium-standalone",
  "main": "example.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "selenium-standalone": "^6.15.4",
    "webdriverio": "^4.14.0"
  }
}
